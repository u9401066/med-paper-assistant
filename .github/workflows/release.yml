name: Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags: v0.3.0, v1.0.0, etc.
  workflow_dispatch:

permissions:
  contents: write  # For creating GitHub Releases

jobs:
  # ──────────────────────────────────────────────────────
  # 1. Validate tag version matches source files
  # ──────────────────────────────────────────────────────
  validate:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$TAG_VERSION" >> "$GITHUB_OUTPUT"
          echo "📌 Tag version: $TAG_VERSION"

      - name: Validate pyproject.toml version
        run: |
          PYPI_VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PYPI_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Version mismatch: pyproject.toml=$PYPI_VERSION, tag=$TAG_VERSION"
            echo "   Run: ./scripts/bump-version.sh $TAG_VERSION"
            exit 1
          fi
          echo "✅ pyproject.toml version: $PYPI_VERSION"

      - name: Validate package.json version
        run: |
          VSX_VERSION=$(node -p "require('./vscode-extension/package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$VSX_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Version mismatch: package.json=$VSX_VERSION, tag=$TAG_VERSION"
            echo "   Run: ./scripts/bump-version.sh $TAG_VERSION"
            exit 1
          fi
          echo "✅ package.json version: $VSX_VERSION"

  smoke-cross-platform:
    name: "Cross-Platform Smoke (${{ matrix.os }})"
    runs-on: ${{ matrix.os }}
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python dependencies
        shell: bash
        run: |
          uv sync --all-extras
          uv pip install -e integrations/pubmed-search-mcp
          uv pip install -e integrations/cgu

      - name: Install VS Code extension dependencies
        working-directory: vscode-extension
        run: npm ci

      - name: Verify integration helper (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          ./scripts/setup-integrations.sh
          ./scripts/start-drawio.sh

      - name: Verify integration helper (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          ./scripts/setup-integrations.ps1
          ./scripts/start-drawio.ps1

      - name: Run Python smoke tests
        shell: bash
        run: uv run pytest tests/test_export_pipeline.py tests/test_figure_embedding.py tests/test_config_paths.py -q

      - name: Run VS Code extension smoke tests
        working-directory: vscode-extension
        run: npm test -- --run src/test/uvManager.test.ts src/test/packaging.test.ts

  # ──────────────────────────────────────────────────────
  # 2. Run tests before publishing
  # ──────────────────────────────────────────────────────
  test-python:
    runs-on: ubuntu-latest
    needs: [validate, smoke-cross-platform]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          uv sync --all-extras
          uv pip install -e integrations/pubmed-search-mcp
          uv pip install -e integrations/cgu

      - name: Run tests
        run: uv run pytest tests/ -x -q --timeout=60 -m "not integration and not slow"

  test-vsx:
    runs-on: ubuntu-latest
    needs: [validate, smoke-cross-platform]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: vscode-extension
        run: npm ci

      - name: Sync bundled skills & prompts
        run: |
          cd vscode-extension
          for skill in literature-review concept-development concept-validation \
                       parallel-search project-management draft-writing \
                       reference-management word-export auto-paper \
                       academic-debate idea-validation manuscript-review \
                       submission-preparation git-precommit; do
            if [ -d "../.claude/skills/$skill" ]; then
              mkdir -p "skills/$skill"
              cp "../.claude/skills/$skill/SKILL.md" "skills/$skill/"
            fi
          done
          for prompt in mdpaper.write-paper mdpaper.literature-survey \
                        mdpaper.manuscript-revision mdpaper.search \
                        mdpaper.concept mdpaper.draft mdpaper.project \
                        mdpaper.format mdpaper.strategy mdpaper.analysis \
                        mdpaper.clarify mdpaper.help; do
            if [ -f "../.github/prompts/$prompt.prompt.md" ]; then
              cp "../.github/prompts/$prompt.prompt.md" "prompts/"
            fi
          done
          if [ -f "../.github/prompts/_capability-index.md" ]; then
            cp "../.github/prompts/_capability-index.md" "prompts/"
          fi
          if [ -f "../.github/copilot-instructions.md" ]; then
            cp "../.github/copilot-instructions.md" "copilot-instructions.md"
          fi
          # Sync templates
          mkdir -p templates
          if [ -f "../templates/journal-profile.template.yaml" ]; then
            cp "../templates/journal-profile.template.yaml" "templates/"
          fi

      - name: Run tests
        working-directory: vscode-extension
        run: npm test

      - name: Compile TypeScript
        working-directory: vscode-extension
        run: npm run compile

  # ──────────────────────────────────────────────────────
  # 3. Publish to PyPI (Trusted Publishing)
  # ──────────────────────────────────────────────────────
  publish-pypi:
    runs-on: ubuntu-latest
    needs: [validate, smoke-cross-platform, test-python]
    environment: pypi
    permissions:
      id-token: write  # Required for trusted publishing
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
        # Uses trusted publishing - no API token needed!
        # Configure at: https://pypi.org/manage/project/med-paper-assistant/settings/publishing/

  # ──────────────────────────────────────────────────────
  # 4. Publish to VS Code Marketplace
  # ──────────────────────────────────────────────────────
  publish-vsx:
    runs-on: ubuntu-latest
    needs: [validate, smoke-cross-platform, test-vsx]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: vscode-extension
        run: npm ci

      - name: Sync bundled skills & prompts
        run: |
          cd vscode-extension
          # Copy skills from source
          for skill in literature-review concept-development concept-validation \
                       parallel-search project-management draft-writing \
                       reference-management word-export auto-paper \
                       academic-debate idea-validation manuscript-review \
                       submission-preparation git-precommit; do
            if [ -d "../.claude/skills/$skill" ]; then
              mkdir -p "skills/$skill"
              cp "../.claude/skills/$skill/SKILL.md" "skills/$skill/"
            fi
          done
          # Copy prompts from source
          for prompt in mdpaper.write-paper mdpaper.literature-survey \
                        mdpaper.manuscript-revision mdpaper.search \
                        mdpaper.concept mdpaper.draft mdpaper.project \
                        mdpaper.format mdpaper.strategy mdpaper.analysis \
                        mdpaper.clarify mdpaper.help; do
            if [ -f "../.github/prompts/$prompt.prompt.md" ]; then
              cp "../.github/prompts/$prompt.prompt.md" "prompts/"
            fi
          done
          if [ -f "../.github/prompts/_capability-index.md" ]; then
            cp "../.github/prompts/_capability-index.md" "prompts/"
          fi
          if [ -f "../.github/copilot-instructions.md" ]; then
            cp "../.github/copilot-instructions.md" "copilot-instructions.md"
          fi
          # Sync templates
          mkdir -p templates
          if [ -f "../templates/journal-profile.template.yaml" ]; then
            cp "../templates/journal-profile.template.yaml" "templates/"
          fi

      - name: Copy Python MCP source
        run: |
          cd vscode-extension
          # med_paper_assistant
          if [ -d "../src/med_paper_assistant" ]; then
            rm -rf bundled/tool/med_paper_assistant
            cp -r ../src/med_paper_assistant bundled/tool/med_paper_assistant
          fi
          # cgu
          if [ -d "../integrations/cgu/src/cgu" ]; then
            rm -rf bundled/tool/cgu
            cp -r ../integrations/cgu/src/cgu bundled/tool/cgu
          fi

      - name: Compile & test
        working-directory: vscode-extension
        run: |
          npm run compile
          npm test

      - name: Package VSIX
        working-directory: vscode-extension
        run: npx @vscode/vsce package

      - name: Publish to VS Marketplace
        working-directory: vscode-extension
        run: npx @vscode/vsce publish -p ${{ secrets.VSCE_PAT }}

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: medpaper-assistant-${{ needs.validate.outputs.version }}.vsix
          path: vscode-extension/*.vsix

  # ──────────────────────────────────────────────────────
  # 5. Create GitHub Release
  # ──────────────────────────────────────────────────────
  github-release:
    runs-on: ubuntu-latest
    needs: [validate, publish-pypi, publish-vsx]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: medpaper-assistant-${{ needs.validate.outputs.version }}.vsix

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: "*.vsix"
          body: |
            ## 📦 Installation

            ### PyPI
            ```bash
            pip install med-paper-assistant==${{ needs.validate.outputs.version }}
            ```

            ### VS Code Marketplace
            Search "MedPaper Assistant" in VS Code Extensions, or:
            ```bash
            code --install-extension u9401066.medpaper-assistant
            ```
