---
description: Export draft and figures to a Word document using a template.
---

1. **Select Draft**: Ask the user which draft file to format.
2. **Select Template**: Ask the user which Word template to use (from `templates/`).
3. **Export**: Use the `export_word` tool to generate the `.docx` file.
