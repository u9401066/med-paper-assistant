---
name: concept-development
description: 發展研究概念並驗證創新性。觸發：concept、發展概念、研究概念、novelty、創新點、補充概念、幫我補充、擴展概念、加強、驗證失敗、novelty不夠、怎麼改。
---

# 研究概念發展技能

## 核心工具

| 工具 | 用途 |
|------|------|
| `read_draft` / `write_draft` | 讀寫 concept.md |
| `validate_for_section` | ⭐ 針對特定 section 驗證（推薦）|
| `validate_concept` | 完整驗證（三輪 ≥75 分）|
| `list_saved_references` | 檢視已收集文獻 |
| `save_reference_mcp` | 儲存新文獻（**永遠優先**）|
| `sync_references` | 同步 wikilinks 到 References |
| `mcp_cgu_deep_think` | CGU 深度分析概念缺口 |

---

## 流程 A：從零建立

```
1. get_current_project() → 確認專案
2. list_saved_references() → 回顧文獻
3. 定義 PICO + 撰寫 NOVELTY + SELLING_POINTS
4. write_draft(filename="concept.md", content="...")
5. validate_for_section(section="Introduction") → 檢查能否開始寫
```

## 流程 B：補充現有 Concept

**觸發**：「幫我補充」「加強 concept」「擴展」

```
1. read_draft("concept.md") → 了解現狀
2. mcp_cgu_deep_think(topic="分析弱點：[概念摘要]") → 找缺口
3. search_literature(query="[需支持的論點]") → 補充文獻
4. save_reference_mcp(pmid="xxx") → 儲存
5. 更新 concept.md，加入 [[citation_key]]
6. sync_references("concept.md") → 同步引用
```

---

## 🚨 驗證失敗處理流程

**觸發**：「驗證失敗」「novelty 不夠」「怎麼改」「score 太低」

### Step 1: 診斷問題

```
呼叫：validate_concept()
# 看回傳的 errors 和 suggestions
```

### Step 2: 常見問題與修復

| 問題 | 診斷 | 修復方式 |
|------|------|----------|
| **Novelty < 75** | 創新點太模糊 | 細化：加上具體對象/方法/指標 |
| **Novelty < 75** | 缺乏文獻支持 | 搜尋確認「首次」的說法 |
| **SELLING_POINTS 不足** | 少於 3 個 | 從文獻中提取更多優勢 |
| **Missing sections** | 結構不完整 | 填補缺少的區塊 |

### Step 3: 修復策略

**A. Novelty 太弱？用 CGU 分析：**
```
mcp_cgu_deep_think(
    topic="如何強化這個研究的創新性？[貼上 NOVELTY STATEMENT]",
    depth="medium"
)
```

**B. 需要更多文獻支持？**
```
search_literature(query="[創新點關鍵字] NOT review")
# 找到後儲存並加入引用
```

**C. 重新定義範圍：**
- 縮小對象（特定族群）
- 改變指標（更創新的 outcome）
- 換個角度（方法創新而非結果創新）

### Step 4: 迭代直到通過

```
write_draft("concept.md", updated_content)
validate_concept()  # 重新驗證
# 如果還是失敗，回到 Step 2
```

### ⚠️ 何時該停下來問用戶？

| 情況 | Agent 行為 |
|------|------------|
| 連續 3 次驗證失敗 | **停下來**，列出問題請用戶決定方向 |
| 核心 NOVELTY 需重大修改 | **停下來**，因為這會改變研究方向 |
| 找不到支持文獻 | **停下來**，可能需要重新定義研究問題 |
| 用戶說「先這樣」 | 尊重用戶決定，記錄在 .memory/ |

---

## 分層驗證（快速參考）

| Paper Type | Core Required | Intro | Methods |
|------------|---------------|-------|---------|
| original-research | NOVELTY, SELLING_POINTS | background, gap | ⚠️ recommended |
| systematic-review | + search_strategy | same | same |
| case-report | same | same | minimal |
| letter | NOVELTY only | minimal | - |

**Section-Specific 驗證**：
```
validate_for_section(section="Introduction")
# → can_write_section=True 就能開始寫，Methods 缺少不 blocking
```

---

## Wikilink 格式

| 格式 | 範例 | 狀態 |
|------|------|------|
| ✅ 正確 | `[[ruetzler2024_38497992]]` | 標準 |
| ❌ 錯誤 | `[[38497992]]` | 缺 author_year |

**取得 citation_key**：
```
save_reference_mcp(pmid="38497992")
# 回傳：💡 Use [[ruetzler2024_38497992]]
```

---

## 好的 Novelty Statement 範例

✅ 「這是首次在**台灣 ICU** 比較 **remimazolam 和 propofol**」
✅ 「首次使用**機器學習**預測 **X 的 Y**」
❌ 「我們研究了 X」（太模糊）
❌ 「這是一項重要的研究」（沒有具體創新點）

---

## 決策點

| 時機 | 問題 | 選項 |
|------|------|------|
| 驗證失敗 | 怎麼辦？ | 修改概念 / 補充文獻 / **問用戶** |
| 創新角度 | 選哪個？ | 新對象/新方法/新指標/更大樣本 |
| CGU 工具 | 用哪個？ | deep_think（分析）/ generate_ideas（發想）|

---

## 相關技能

- `literature-review` - 前置步驟
- `parallel-search` - 補充文獻搜尋
